import 'package:flutter/material.dart';

class AppColors {
  // Pick your brand seed (Material 3 generates the full palette from this)
  static const Color seed = Color(0xFF2F6FED);

  // Optional: keep a few brand colors if you need them directly in UI
  static const Color success = Color(0xFF1B8A5A);
  static const Color danger = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFF9A825);

  const AppColors._();
}